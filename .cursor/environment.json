{
  "name": "contentanalysis",
  "install": "sudo apt-get update -qq && sudo apt-get install -y python3.12-venv python3-dev build-essential && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "streamlit",
      "command": ".venv/bin/streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true"
    }
  ],
  "ports": [
    {
      "name": "streamlit",
      "port": 8501
    }
  ]
}
